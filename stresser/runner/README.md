# Runner
The thing that actually runs a program defined by a single source file and language identifier.  
Should be ran with 2 arguments - "file path" and "language id". File path should be path to file in the container, so it should be attached as a volume to the local file path.